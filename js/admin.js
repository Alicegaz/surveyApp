(function(){
    'use strict';
    var app = angular
        .module('app')
        .controller('AdminController', AdminController);

        AdminController.$inject = ['$window', '$rootscope', '$log', '$timeout', '$stateProvider'];

        function AdminController($window, $rootscope, $log, $timeout, $stateProvider)
        {

        }
})();